#!/usr/bin/env bash
# Every test, in one command. This is the string that appears in each task file's
# **Verification:** field, so it has to be the thing that actually runs.
set -euo pipefail
cd "$(dirname "$0")"
exec python3 -m unittest discover -s tests -t . -v
