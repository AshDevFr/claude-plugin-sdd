#!/usr/bin/env python3
"""Entry point. Kept extensionless so the command reads as `./beacon render ...`."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
from beacon.cli import main

sys.exit(main())
